{
	"cniVersion": "0.2.0",
	"name": "bridge",
	"type": "bridge",
	"bridge": "cni0",
	"isGateway": true,
	"ipMasq": true,
	"ipam": {
		"type": "host-local",
		"subnet": "{{ pod_cidr }}",
		"routes": [
			{ "dst": "0.0.0.0/0" }
		]
	}
}
