{
    "CN": "kube-apiserver",
    "hosts": [
        "{{ hostvars[item].ansible_host}}",
        "{{ item }}",
        "{{ kubernetes_service }}",
        "{{ advertise_ip_masters }}",
        "kubernetes",
        "kubernetes.default",
        "kubernetes.default.svc",
        "kubernetes.default.svc.cluster",
        "kubernetes.default.svc.cluster.local"{%- if custom_dns_alt_name -%},
        "{{ custom_dns_alt_name }}"
        {% endif -%}
    ],
    "key": {
        "algo": "rsa",
        "size": 2048
    },
    "names": [
        {
            "C": "{{ c }}",
            "ST": "{{ st }}",
            "L": "{{ l }}"
        }
    ]
}
