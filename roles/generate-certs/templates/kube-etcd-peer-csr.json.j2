{
    "CN": "kube-etcd-peer",
    "hosts": [
        "localhost",
        "127.0.0.1",
        "{{ hostvars[item].ansible_host}}",
        "{{ item }}"
    ],
    "key": {
        "algo": "rsa",
        "size": 2048
    },
    "names": [
        {
            "C": "{{ c }}",
            "ST": "{{ st }}",
            "L": "{{ l }}"
        }
    ]
}
