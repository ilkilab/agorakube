{
    "CN": "kube-etcd-peer",
    "hosts": [
        "localhost",
        "127.0.0.1",
        "{{ hostvars[item].ip}}",
        "{{ hostvars[item].hostname}}"
    ],
    "key": {
        "algo": "rsa",
        "size": 2048
    },
    "names": [
        {
            "C": "{{ c }}",
            "ST": "{{ st }}",
            "L": "{{ l }}"
        }
    ]
}
